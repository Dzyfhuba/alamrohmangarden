import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hafidzubaidillah.alamrohmangarden',
  appName: 'Alam Rohman Garden',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
